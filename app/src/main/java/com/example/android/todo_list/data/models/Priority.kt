package com.example.android.todo_list.data.models
//enum class is a data type which consists of fixed set of constants when we require predefined set of value
enum class Priority {
    HIGH,
    MEDIUM,
    LOW
}